pipeline {
    agent {
        docker {
            image 'httpd:latest'
            reuseNode true
        }
    }

    stages {
        stage('Ejecución del contenedor') {
            steps {
                script {
                    sh 'echo "Ejecutando el contenedor..."'
                    sh 'docker run -d -p 8080:80 --name apache-container httpd:latest'
                }
            }
        }
    }

    post {
        always {
            cleanDocker()
        }
    }
}